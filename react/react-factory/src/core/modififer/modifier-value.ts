import type { Nullish } from "@positron/core";

/**
 * The {@link ModifierValue} type represents a type that can be a modifier
 * value.
 *
 * @public
 */
export type ModifierValue = Nullish<string>;
