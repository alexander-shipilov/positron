import type { ReactProps } from "@positron/react-core";

/**
 * The {@link BlockValue} type represents a type of value stored by the
 * block descriptor.
 *
 * @public
 */
export type BlockValue = ReactProps;
