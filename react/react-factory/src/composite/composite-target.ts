import type { AnyObject } from "@positron/core/src";

/**
 * The {@link CompositeTarget} type represents a value that can be described by
 * the {@link CompositeDescriptor}
 *
 * @public
 */
export type CompositeTarget = AnyObject;
