import type { ReactProps } from "@positron/react-core";

/**
 * The {@link BlockDescriptorTarget} type represents a type of value that can be
 * described by the {@link BlockDescriptor}.
 *
 * @public
 */
export type BlockDescriptorTarget = ReactProps;
