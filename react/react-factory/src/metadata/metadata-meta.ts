import type { UnknownObject } from "@positron/core/src";

/**
 * Type {@link MetadataMeta} represents a metadata type that can be added to the
 * {@link MetadataDescriptor}.
 *
 * @public
 */
export type MetadataMeta = UnknownObject;
