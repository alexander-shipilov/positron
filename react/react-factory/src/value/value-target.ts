import type { DescribedTarget } from "../described";

/**
 * The {@link ValueTarget} type represents a value that can be described by
 * the {@link ValueDescriptor}
 *
 * @public
 */
export type ValueTarget = DescribedTarget;
