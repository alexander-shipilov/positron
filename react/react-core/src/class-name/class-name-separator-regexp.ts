/**
 * The {@link CLASS_NAME_SEPARATOR_REGEXP} represents a regular expression to
 * split class names' list to the separate items.
 *
 * @public
 */
export const CLASS_NAME_SEPARATOR_REGEXP = /\s+/;
