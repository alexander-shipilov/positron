/**
 * The {@link NominalTypeSymbol} constant declaration represents a unique symbol is
 * used by `@positron/nominal-type` package.
 *
 * @internal
 */
declare const NominalTypeSymbol: unique symbol;

/**
 * The {@link NominalTypeSymbol} type represents a unique symbol is used by
 * `@positron/nominal-type` package.
 *
 * @public
 */
export type NominalTypeSymbol = typeof NominalTypeSymbol;