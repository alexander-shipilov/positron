/**
 * The {@link NominalSymbol} constant declaration represents a unique symbol is
 * used by `@positron/nominal` package.
 *
 * @internal
 */
export declare const NominalSymbol: unique symbol;

/**
 * The {@link NominalSymbol} type represents a unique symbol is used by
 * `@positron/nominal` package.
 *
 * @public
 */
export type NominalSymbol = typeof NominalSymbol;
