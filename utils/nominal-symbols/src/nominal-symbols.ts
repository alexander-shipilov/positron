/**
 * The {@link NominalSymbols} static class represents well-known symbols used my
 * `nominal` package.
 *
 * @public
 */
export declare class NominalSymbols {
  /**
   * The {@link NominalSymbols.type} represents a symbol to store type.
   *
   * @public
   */
  declare static readonly type: unique symbol;

  private constructor();
}
