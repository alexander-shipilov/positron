/**
 * The {@link MetaTypeSymbol} constant declaration represents a unique symbol is
 * used by `@positron/meta-type` package.
 *
 * @internal
 */
export declare const MetaTypeSymbol: unique symbol;

/**
 * The {@link MetaTypeSymbol} type represents a unique symbol is used by
 * `@positron/meta-type` package.
 *
 * @public
 */
export type MetaTypeSymbol = typeof MetaTypeSymbol;
