import type { FbUtils } from "../types";

import { global } from "./global";

/**
 * The {@link utils}: {@link FbUtils} constant represents a reference to the
 * utils object.
 *
 * @public
 */
export const utils: FbUtils = global.utils;
