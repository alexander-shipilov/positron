import type { FbGdi } from "../types";

import { global } from "./global";

/**
 * The {@link gdi}: {@link FbGdi} constant represents a reference to the GDI
 * object.
 *
 * @public
 */
export const gdi: FbGdi = global.gdi;
