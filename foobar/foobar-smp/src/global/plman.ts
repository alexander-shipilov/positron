import type { FbPlaylistManager } from "../types";

import { global } from "./global";

/**
 * The {@link plman}: {@link FbPlaylistManager} constant represents a reference
 * to the playlist manager object.
 *
 * @public
 */
export const plman: FbPlaylistManager = global.plman;
