import type { FbWindow } from "../types";

import { global } from "./global";

/**
 * The {@link window}: {@link FbWindow} constant represents a reference to the
 * window object.
 *
 * @public
 */
export const window: FbWindow = global.window;
