import type { FooBar } from "../types";

import { global } from "./global";

/**
 * The {@link fb}: {@link FooBar} constant represents a reference to the foobar
 * object.
 *
 * @public
 */
export const fb: FooBar = global.fb;
