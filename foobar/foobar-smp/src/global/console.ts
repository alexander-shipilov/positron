import type { FbConsole } from "../types";

import { global } from "./global";

/**
 * The {@link console}: {@link FbConsole} constant represents a reference to
 * the console object.
 *
 * @public
 */
export const console: FbConsole = global.console;
